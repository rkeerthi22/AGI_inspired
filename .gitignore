# Secrets — never commit
.env
*.key
secrets/

# Live binaries — backed up separately (nightly), not version-controlled
ledger/*.db
ledger/*.db-shm
ledger/*.db-wal
memory/*.db
memory/*.db-shm
memory/*.db-wal

# Agent scratch + ingested raw data (audit lives in the ledger, not here)
workspace/
inbox/
runs/
*.log

# Python
__pycache__/
*.pyc
.venv/

# OS
Thumbs.db
desktop.ini
